# Sensitive
wireguard/
github-copilot/
karabiner/
kanata/

# Runtime noise
.DS_Store
zsh/.zcompdump
zsh/.zcompcache/
zsh/.zsh_sessions/
vim/undodir/
vim/.netrwhistory
kak/plugins/

# Zed runtime
zed/prompts/*
zed/settings_backup.json
zed/config_bak

# Ghostty shaders (submodule/downloaded, not owned)
ghostty/ghostty-shaders/

# Machine-specific (not committed)
fish/config.local.fish

# Abandoned / not stowed (leave in ~/.config but don't track)
m0tay/
m0tay2/
